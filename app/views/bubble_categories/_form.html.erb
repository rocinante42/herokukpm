<%= form_for(@bubble_category) do |f| %>
  <% if @bubble_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bubble_category.errors.count, "error") %> prohibited this bubble_category from being saved:</h2>

      <ul>
      <% @bubble_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
