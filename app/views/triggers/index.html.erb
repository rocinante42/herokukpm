<h1>Listing triggers</h1>

<table>
  <thead>
    <tr>
      <th>Bubble</th>
      <th>Bubble Group</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @triggers.each do |trigger| %>
      <tr>
        <td><%= link_to trigger.bubble.try(:name), trigger.bubble %></td>
        <td><%= link_to trigger.bubble_group.try(:name), trigger.bubble_group %></td>
        <td><%= link_to 'Show', trigger %></td>
        <td><%= link_to 'Edit', edit_trigger_path(trigger) %></td>
        <td><%= link_to 'Destroy', trigger, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trigger', new_trigger_path %>
